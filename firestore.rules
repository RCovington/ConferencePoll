rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all client-side access — only server-side (Admin SDK) can read/write
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
